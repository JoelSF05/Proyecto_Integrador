# Configuración de PostgreSQL — credenciales por variables de entorno
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-us-west-2.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SUPABASE_DB_USER:postgres.tulotsjlvhmyjhnxeqdz}
spring.datasource.password=${SUPABASE_DB_PASS:bd_integrador123}

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Puerto del servidor
server.port=8080

# Thymeleaf
spring.thymeleaf.cache=true

# Logging — producción: solo WARN
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN
